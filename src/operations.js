const add = (param2, param3) => param2 + param3;

const subtract = (param2, param3) => param2 - param3;

const multiply = (param2, param3) => param2 * param3;

const divide = (param2, param3) => param2 / param3;


module.exports = {
    add,
    subtract,
    multiply,
    divide
};
